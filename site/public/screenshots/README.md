# Landing screenshot provenance

The initial five PNG screenshots were manually supplied by the user on 2026-09-16. Originals were copied unchanged from the user's Qterm截图 folder. Each is 1206 × 737 pixels, showing the fictional browser Demo.

- workspace.png: build terminal, Git changes, file directory and network rules.
- files.png: local terminal, file directory and README preview.
- network.png: SOCKS5 proxy rule editor with listening address and port.
- git.png: repository, branch, changed file, staging and commit controls.
- export.png: full image export dialog, Windows window style, cyber theme and output-size controls.

The introduction preserves the complete images without cropping or lossy conversion. Width/height attributes reserve each image’s native aspect ratio. The export section uses a full-width image to keep the complete dialog readable. Screenshot links open the shared in-page image viewer, with a frameless full-image view, double-click/double-tap zoom, mouse drag/native touch scrolling, and fading scroll indicators; without JavaScript they fall back to the original PNG in the same tab. Keep dimensions, captions and alt text in site/index.html synchronized when replacing images.


Eight additional desktop screenshots were supplied by the user on 2026-09-18. Each is 1165 × 709 pixels; originals remain unchanged. They show desktop features, not browser Demo capabilities.

- Vibe Coding: paste-image-to-cli.png (paste images to a remote terminal), notification.png (Agent OSC notification from another session), paste-file-to-cli.png (upload and insert a remote file path).
- Files and Git: drag-file-upload.png (drop files into the current remote directory), git-diff.png (side-by-side code diff).
- Connections and credentials: ssh-jump.png (experimental multi-hop SSH configuration), key-vault-manager.png (password/private-key management), key-vault.png (unlock and select credentials for the current connection).

Each feature group has independent tabs. The existing files.png and git.png remain in the workflow group; network.png is now in the connections group. All screenshots use the shared image viewer with native dimensions and descriptive alt text.
